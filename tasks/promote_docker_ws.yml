- hosts: win
  tasks:
    - name: Build Docker container
      win_shell: docker tag sebastianluen/ws:{{ src_tag }} sebastianluen/ws:{{ tgt_tag }}
      register: output
    
    - name: Debug the output
      debug:
        var: output
