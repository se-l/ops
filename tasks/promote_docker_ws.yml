- hosts: win
  tasks:
    - name: Build Docker container
      win_shell: docker tag sebastianluen/ws:{{ lookup('env', 'SRC_TAG') }} sebastianluen/ws:{{ lookup('env', 'TGT_TAG') }}
      register: output
    
    - name: Debug the output
      debug:
        var: output
