- hosts: aws
  tasks:
    - name: restart a particular instance by its ID
      amazon.aws.ec2_instance:
      state: restarted
      instance_ids:
      - i-006852abccae49b60
      access_key: "{{ lookup('env', 'AWS_SECRET') }}"